import React from "react";

const Banner = (props) => {
    return (
        <header className="header container parallax bg-gradient-to-b from-yellow-400 to-red-500">
            <p>Personal finance done easy.</p>
        </header>
    );
};

export default Banner;
